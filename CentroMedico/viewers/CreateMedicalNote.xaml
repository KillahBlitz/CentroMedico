<Window x:Class="CentroMedico.viewers.CreateMedicalNote"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Nueva Nota de Evolución" Height="600" Width="800"
        WindowStartupLocation="CenterScreen" 
        WindowStyle="None" ResizeMode="NoResize"
        AllowsTransparency="True" Background="Transparent">

    <Window.Resources>
        <Style TargetType="TextBlock" x:Key="LabelStyle">
            <Setter Property="Foreground" Value="#374151"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <Style TargetType="Control" x:Key="InputStyle">
            <Setter Property="Height" Value="40"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Control">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox" x:Key="TextAreaStyle">
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="VerticalContentAlignment" Value="Top"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="AcceptsReturn" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="White" CornerRadius="12" Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.2"/>
        </Border.Effect>

        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,20">
                <TextBlock Text="Nueva Nota de Evolución" FontSize="22" FontWeight="Bold" Foreground="#1F2937"/>
                <Button HorizontalAlignment="Right" Click="CloseModal" Background="Transparent" BorderThickness="0" Cursor="Hand">
                    <TextBlock Text="✕" FontSize="20" Foreground="#9CA3AF"/>
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <ContentPresenter/>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="0,0,10,0">
                <StackPanel>
                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Fecha" Style="{StaticResource LabelStyle}"/>
                            <DatePicker x:Name="dateInput" Height="40" VerticalContentAlignment="Center" />
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Tipo de Consulta" Style="{StaticResource LabelStyle}"/>
                            <TextBox x:Name="consultationTypeInput" Style="{StaticResource InputStyle}"/>
                        </StackPanel>
                    </Grid>

                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Peso (kg)" Style="{StaticResource LabelStyle}"/>
                            <TextBox x:Name="weightInput" Style="{StaticResource InputStyle}" PreviewTextInput="OnlyNumbers_PreviewTextInput" MaxLength="6"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Talla (cm)" Style="{StaticResource LabelStyle}"/>
                            <TextBox x:Name="heightInput" Style="{StaticResource InputStyle}" PreviewTextInput="OnlyNumbers_PreviewTextInput" MaxLength="5"/>
                        </StackPanel>
                        <StackPanel Grid.Column="4">
                            <TextBlock Text="P.C. (cm)" Style="{StaticResource LabelStyle}"/>
                            <TextBox x:Name="pcInput" Style="{StaticResource InputStyle}" PreviewTextInput="OnlyNumbers_PreviewTextInput" MaxLength="5"/>
                        </StackPanel>
                        <StackPanel Grid.Column="6">
                            <TextBlock Text="T° (°C)" Style="{StaticResource LabelStyle}"/>
                            <TextBox x:Name="tempInput" Style="{StaticResource InputStyle}" PreviewTextInput="OnlyNumbers_PreviewTextInput" MaxLength="4"/>
                        </StackPanel>
                    </Grid>

                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="FC (lpm)" Style="{StaticResource LabelStyle}"/>
                            <TextBox x:Name="fcInput" Style="{StaticResource InputStyle}" PreviewTextInput="OnlyNumbers_PreviewTextInput" MaxLength="3"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="FR (rpm)" Style="{StaticResource LabelStyle}"/>
                            <TextBox x:Name="frInput" Style="{StaticResource InputStyle}" PreviewTextInput="OnlyNumbers_PreviewTextInput" MaxLength="3"/>
                        </StackPanel>
                    </Grid>

                    <StackPanel Margin="0,0,0,15">
                        <TextBlock Text="Nota de evolución" Style="{StaticResource LabelStyle}"/>
                        <TextBox x:Name="evolutionNoteInput" Style="{StaticResource TextAreaStyle}" Height="80"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,0,15">
                        <TextBlock Text="Estudios de apoyo / Complementarios" Style="{StaticResource LabelStyle}"/>
                        <TextBox x:Name="studiesInput" Style="{StaticResource TextAreaStyle}" Height="60"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,0,15">
                        <TextBlock Text="Diagnóstico y tratamiento" Style="{StaticResource LabelStyle}"/>
                        <TextBox x:Name="diagnosisInput" Style="{StaticResource TextAreaStyle}" Height="80"/>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button Content="Cancelar" Click="CloseModal" 
                        Background="White" Foreground="#374151" BorderBrush="#D1D5DB" BorderThickness="1" 
                        Width="120" Height="40" Margin="0,0,15,0" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <Button Content="Guardar Nota" Click="SaveNote" Background="#2563EB" Foreground="White" BorderThickness="0" Width="150" Height="40" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="6">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" TextElement.FontWeight="Bold"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>